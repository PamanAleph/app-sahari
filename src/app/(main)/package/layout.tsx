// src/app/(main)/friends/package/layout.tsx
export default function PackageLayout({ children }: { children: React.ReactNode }) {
    return (
      <div className="scroll-smooth">
        {children}
      </div>
    );
  }
  